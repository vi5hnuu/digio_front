import React from 'react'

// export default function CrouselDot() {
//   return <div onClick={() => { setSelectedProductIdx(0) }} className={`${styles.dot} ${selectedProductIdx === 0 ? styles.active : ''}`}></div>
// }
